<h2>Welcome aboard!</h2>
<p>Hi <strong>{{name}}</strong>, your account is ready. Use the credentials below to login:</p>

<div class="card">
    <table>
        <tr>
            <td class="card-label">Email</td>
            <td class="card-value">{{email}}</td>
        </tr>
        <tr>
            <td class="card-label">Password</td>
            <td class="card-value">{{password}}</td>
        </tr>
    </table>
</div>

<div class="btn-container">
    <a href="{{appUrl}}" class="btn" style="color: #ffffff !important; text-decoration: none;">Login to ezFLM</a>
</div>